/**
 * LEGO Story Mobile App - 入口文件
 *
 * 此文件作为应用的入口点，只负责导出 src/App.js 中的默认导出。
 * 所有应用逻辑、组件、屏幕和导航都定义在 src/ 目录下，遵循标准 React Native 架构。
 *
 * @module App
 */

import App from './src/App';

export default App;
